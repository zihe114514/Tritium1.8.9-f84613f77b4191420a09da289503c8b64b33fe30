# 更新日志

本文件记录 MuoniumPlayer 各版本的变更。格式参考 Keep a Changelog，版本号遵循语义化版本。

## [1.2.0] - 2026-08-23

以歌词逐字渲染为主的中型更新：修掉桌面歌词的字形发虚与 KTV 染色显示不全，给全屏歌词加上随「这个字已经唱了多久」渐强的逐字发光并配一张可实时预览的快捷配置面板；另外补上常驻灵动岛，以及歌曲列表右侧那一组控件的横向对齐。

### 新增

- **常驻灵动岛**。HUD 编辑器「灵动岛」分组新增「常驻灵动岛」总开关，打开后即使没有下载和通知，灵动岛也留在屏幕顶部当一条实时状态栏；「常驻 · 帧率 / 常驻 · 延迟 / 常驻 · 时间」三个子开关各自独立，可以只留想看的那几项。
  - 显示内容：帧率（`Minecraft.getDebugFPS()`）、当前服务器延迟（从 `NetworkPlayerInfo.getResponseTime()` 取，单机或未进服时显示 `--`）、系统时间（时:分）。数值按健康度着色（帧率 ≥90 / ≥45 / ≥25 分四档，延迟 ≤60ms / ≤120ms / ≤250ms 分四档），中性档接近纯白，避免把"正常"画成告警色。
  - **通知不重播入场动画**。常驻岛已经稳定停在屏幕上时（`visibility > .55`），新通知与新下载都不再重置入场时钟与内容过渡，只做常驻内容与通知内容之间的交叉淡入；只有"刚打开常驻"那一次会照常播一遍入场。此前每来一条通知都会重置 `shownAt`，在常驻态下表现为灵动岛原地抽动一下。
  - **完整沿用原有排队机制**。常驻不是一种通知，它不进 `NOTICE_QUEUE`，也不参与优先级判定；显示与否的四条理由里只是多了"常驻"这一条，下载 / 完成停留 / 通知三条原有理由的判定与顺序一字未改，因此队列间隔、最低优先级（网易云听歌时长上报）、持久卡片超时全部保持原样。通知一来常驻内容立刻让位，通知走完再自动回到常驻。
  - **宽度不抖动**。帧率和延迟每秒都在变，如果按真实字宽排版，灵动岛会一直微微伸缩。每个条目按固定位数模板（`888` / `8888` / `88:88`）预留宽度、数值在槽内居中，因此常驻状态下宽度完全静止；只有帧率破千这种确实需要多一位的情况才升到四位模板。
  - 常驻内容按 250ms 采样一次而不是跟帧读取，否则数字会闪得没法看。三个子开关全关时灵动岛收成紧凑胶囊，而不是展开一张空白卡片。
  - 常驻与通知的目标尺寸按混合度插值，切换时表面是一次连续变形，不会先塌回去再弹出来。HUD 编辑器的实时预览在打开常驻后直接显示常驻状态栏，开关所见即所得。
  - 常驻状态下左侧换成一枚专用标记（心跳折线 + 来回扫过的亮点）而不是加载转子：常驻并不代表有任务在跑，用转子会误导。
- **全屏歌词的逐字发光**。以前全屏歌词只有“整个字词随演唱脉冲式放大”和“逐字轻微抬升”，没有任何发光；现在每个字自带一层光晕，亮度只由**这个字自己已经唱了多久**决定（0 = 还没唱到，1 = 唱完），所以一句话里的光是跟着人声一个字一个字亮起来的，而不是整句一起亮。
  - 发光和字形一起画进 KTV 的 base 纹理，因此会被同一张填涂遮罩裁剪：未唱的部分连光一起被遮住，光的前沿和染色前沿永远是同一条线，不会出现“字还没染色但已经在发光”。
  - 为此 KTV 纹理四周多留了 6 逻辑像素空白（发光是向外扩的，不留白会被纹理边界切成一个方块）。留白取整数，`fbWidth / 2`、`fbHeight / 2` 仍是整数，合成四边形与纹理保持 1:1，不引入额外重采样；把发光调到 0 时留白就是 0，此时 FBO 尺寸、遮罩坐标与合成四边形和加发光之前完全等价（可退回验证）。
  - 字词唱完（进度 ≥ 0.999）之后不再走 KTV 纹理合成，那条直接绘制的分支也补上了满亮度发光，否则字词唱完的那一帧光会突然消失。
  - 1.8.9 没有可用的后处理管线，光晕沿用项目里其它发光的做法：同一个字形按环形偏移多画几遍、逐层降透明度，免着色器。最大半径 5.1 逻辑像素，放得下 6 像素的留白（已探针核对）。
- **全屏歌词的「A-Z」快捷配置面板**。全屏播放器左上角新增一枚 A-Z 按钮，点开是一张浮层面板，内含两条滑块：
  - **全屏逐字发光**（0.00 ~ 1.00，默认 0.55）：上面那层逐字发光的亮度上限。刻意不复用「发光程度」（`currentGlowStrength`），因为后者同时作用于 OSD 桌面歌词的整行光晕，调大它会把桌面歌词一起搞过曝。
  - **逐字放大**（0.00 ~ 0.28）：这一项本来就存在，但渲染代码里又写死了一层 0.14 的上限，于是滑块拖过一半之后完全没有反应——现在去掉了那层硬夹，量程交给配置层。滑到 0.10（默认值）时行为与以前完全一致。
  - 实时预览不需要额外机制：渲染器每帧直接读配置字段，拖动的同一帧就生效。落盘只在松开鼠标时发生一次，避免拖动过程中反复写文件。点面板外面收起，收起时清掉拖动状态。
  - 同样两项也进了内置 HUD 编辑器的「当前部分」分组（新增「全屏逐字发光」一行），两处改的是同一份配置。

### 改进

- **歌曲列表右侧的图标与时长统一对齐**。反馈是右侧那一组控件对不齐；实测截图后发现纵向其实是对齐的（三枚图标与时长文字的墨迹中心都落在同一条线上），真正的问题是「收藏」比相邻两枚小了约三成、而且形状根本不对——它画出来是一枚五角星，而资源里的 `favorite.png` 是一颗描边爱心。
  - **根因：贴图路径少了前导斜杠。** `Location.getResourceStream()` 用的是 `Location.class.getResourceAsStream(path)`，不带斜杠会被当作相对 `com/muoniumplayer/core/utils/` 的路径解析，于是 `PlayerIconAssets` 里那七张图一张都读不到：每个 `ThemedTextureIconWidget` 每帧都抛一次 `MissingResourceException`，然后静默退回构造时传入的字体兜底字形（收藏退回 `pf16bold` 的 `☆`）。兜底字形与相邻的图标字体既不同形状也不同大小，看上去就是右侧控件参差不齐。项目里另外两处真实的 classpath 贴图（`MusicToast` 的 HUD 图、着色器）一直是带斜杠写的；`muonium/textures/album|music|playlist|user/...` 那些不带斜杠的 Location 只是网络封面的缓存键、从不读 classpath，所以这个写法一直没暴露。
  - 七个常量全部补上前导斜杠（不是只改「收藏」那一个）：同一处笔误，留着另外六个就是留六个每帧抛异常的坑。可见影响有两处 —— 歌曲行的「收藏」现在是设计好的描边爱心，播放条的播放模式按钮现在显示对应贴图而不是字母兜底（`LoopInList` 本来就没有配图，仍走字体，属既有设计）。顺带去掉了列表滚动时每帧数十次的异常构造与失败资源查找。
  - **贴图改用线性采样。** 这些图标是 96×96 画布，要缩进 20 逻辑像素的按钮里（约 4.8 倍缩小），而 `DynamicTexture` 默认是 `GL_NEAREST`：3 像素粗的描边会被整段抽掉，图标看起来缺边少角、也显得比字体图标小。改为绑定后设 `RenderSystem.linearFilter()`，与项目里头像/封面走的是同一条处理。
  - **尺寸本来就是对标好的，不需要再加缩放系数。** `PlayerQueueIcons.TARGET_INK_RATIO = .52` 当初就是从 `favorite.png`（墨迹 52×48，占画布 54.2%/50.0%）与 `playlist.png`（52×50）量出来的。路径修好之后三枚在 20 像素按钮里的墨迹高度是 10.00 / 10.50 / 10.50 逻辑像素，最大差 0.5 像素（在默认 0.88 缩放下不足一个屏幕像素）；宽度 10.83 / 9.5 / 10.5，因此盒子等距排布时视觉间隙也自然匀了。
  - 顺带把 `personal-fm.png` 的墨迹在画布里重新居中（原来偏右 8.5 像素、偏下 2.5 像素）。`ThemedTextureIconWidget` 是按整张画布填满命中框来画的，画布里偏心多少屏幕上就偏心多少；这张图目前没有使用点，属于提前拆掉的陷阱，只是整体平移像素、没有重采样。
  - **补充：还有一半是横向没对齐（后续反馈澄清后修的）。** 上面那几条修的是"一行之内三枚图标彼此参差"，而反馈真正指的是"上下相邻两行之间，图标列左右对不齐"。根因是时长文本用的是**比例字体**：pf14bold 里 `'1'` 只有 3.5 逻辑像素，`'0'` / `'8'` 是 5.0，于是同样五个字符的 `"11:11"` 量出来 16.0、`"00:04"` 量出来 22.0，**宽度能差 6 个逻辑像素**（全量程 0~59:59 实测区间 16.0 ~ 22.0）。而右侧那三枚图标是一路挂在时长标签左边缘上的（`btnLike → lblMusicDuration.getRelativeX()`，另两枚再挂在它上面），所以每一行的图标列都跟着自己那首歌的数字宽度左右漂移，列表末尾几行凑在一起时最明显。
  - 改成给时长留一个**固定宽度的槽位**：图标一律挂在槽位左边缘（列因此是死的），时长文本本身仍然右对齐贴着行的右边缘（数字按右边缘对齐，与主流播放器一致）。槽位按"用最宽的那个数字铺满模板"从字体现量，不写经验常数，以后换字体或改字号都会自己跟着变——实测 MM:SS 槽位 22.0、HH:MM:SS 槽位 34.0，分别正好等于各自量程内的最宽实测值（既不会压住文本，也没有多留一个像素）。
  - 是否留出小时位按这一行自己的时长决定。一个歌单里"有小时"基本是全有或全无（普通歌单一首都没有，混音 / 播客歌单全都有），所以两种常见情况下都是完美对齐；极少数混排歌单里也只是退回到原来的行为、不会更差，而给每一行都无条件留出小时位会白扔 12 个逻辑像素。字形是懒加载的，首帧量不到宽度时槽位为 0（与改动前的表现一致），再用标签自己的实测宽度兜一层底，保证文本永远不会被压到图标下面。
  - **顺带修掉一个既有的重叠**（如需保留原样请说）：标题与艺术家的右侧留白写的是固定 `80 / 40`，那是「加入歌单」与「下一首播放」两枚图标加进来之前的数字，实际占用是 `102 / 54`。也就是说长标题原本会一直滑到最左那枚图标底下 22 个逻辑像素（标题用的是滚动式限宽，看起来就是文字从图标下面穿过去）。现在留白与图标锚点共用同一套算式，两边永远一致；代价是长标题的截断点比以前早约 22 / 14 个逻辑像素。
- **灵动岛左侧图标的可读性修复**。反馈是"图标动效看不清 / 加载不全"，找到三个各自独立的原因：
  - **托盘溢出轮廓**。折叠态的灵动岛只有 16px 高，而图标托盘固定 18.4px 并额外放大 1.08 倍，于是它会顶出胶囊的圆肩——看起来正像图标被裁掉了一半。现在托盘尺寸随岛高收缩（`(height - 3.6) / 托盘基准`，下限 0.40），展开与收起全过程中图标始终整枚落在轮廓内；同时按展开度淡入，紧凑态只留一枚淡淡的圆点，不会在极小的胶囊里塞满高对比图形。
  - **字形跟着呼吸变暗**。原来图标与托盘共用同一个带正弦呼吸的 alpha，字形每 1.6 秒就被压暗一次，这正是"看不清"的主因。现在呼吸只留给托盘，字形常亮（`alpha * .98`），并在托盘下面补一层深色底衬——液态玻璃这类亮表面上，只靠一层薄强调色会让字形糊进背景。
  - **加载转子笔画太细**。转子笔画 1.64px、尾部不透明度低到 .14，在默认 0.88 缩放叠加高 GUI Scale 时慢转速下只能看到一小截在飘。现在笔画加粗到 1.90px、尾部起步抬到 .22、长度随位置递减，并铺一圈极淡的静止轨道和一个领头高亮点，旋转方向和"正在加载"的语义都读得出来。液态玻璃自带的 18 刻度转子不受影响。
- **歌曲行的「添加到歌单」与播放条的「下一首播放列表」换用新的 fontello 字形**：分别是 U+E113（方框加号 + 上方一横）与 U+E144（播放三角 + 三条横线），随包发布为 `muonium/fonts/player-action-icons.ttf`。
  - 两枚图标此前的问题不同：「添加到歌单」用的是 `playlist.png` 贴图（文字兜底是一个 `+`），而播放条右下角的队列开关直接复用了歌曲行那枚「下一首播放」（U+E309）字形 —— 同一个图标既表示"把这一首插到下一首"又表示"打开整张待播列表"，两个动作分不清。现在队列开关有了自己的`播放三角 + 列表`字形，歌曲行的「下一首播放」保持 U+E309 不变。
  - 独立成第三个字体文件而不是并入 `player-queue-icons.ttf`：两者是不同批次的 fontello 导出，各自独立才不会在私有区码位上互相覆盖（已验证两份字体的码位无交集）。
  - 沿用既有的「按实测墨迹挑字号 + 按墨迹居中」构造逻辑，`PlayerQueueIcons` 因此从单字形工厂泛化为多字形工厂。离线实测：加号在 20 像素按钮里落 38 号、墨迹 10.0×10.5（占边长 50.0% 宽 / 52.5% 高），队列图标在 18 像素按钮里落 38 号、墨迹 10.0×9.5（55.6% / 52.8%），与相邻的 `favorite.png`（54.2% / 50.0%）在同一水平，三枚按钮的视觉大小保持统一。
  - `playlist.png` 与 `PlayerIconAssets.PLAYLIST` 保留未删，方便随时切回贴图。

### 修复

- **桌面歌词（OSD）的字形发虚、KTV 染色显示不全**。反馈是“遮罩/字体模糊、KTV 染色效果显示不全”。逐字重写那一版引入了四个各自独立的原因，逐条修掉：
  - **已唱高亮层的纵向裁剪框比字形真正的墨迹矮**。`CFontRenderer.drawString` 内部先 `y -= 2` 再 `scale(0.5)`，一个字形实际占据 `[y - 2, y + getFontHeight() + 2.25]`；旧裁剪框是 `[y - 1, y - 1 + getHeight() + 4]`，上边界比墨迹顶端低了整整 1 逻辑像素，而且行高还被 `getHeight()` 向下取整过。于是**已唱的那部分字顶被削平、未唱的底色层却是完整的**，两层叠起来就是“染色不全”外加一条横向接缝；逐字放大时这道切口还会按倍率一起放大（1.085 倍时切掉 1.085 像素）。现在按真实墨迹盒给出上下各约一像素余量（探针核对：上 1.00、下 1.75 像素，且在任意缩放倍率下裁剪框都严格包住墨迹）。
  - **羽化窗口的量程是照全屏字号定的**。`osdKaraokeTransitionWidth` 的 4~32 是给全屏 pf65bold 用的，直接套在 OSD 的 pf28bold 上时，默认值 14 已经覆盖一个汉字宽度（14.5 逻辑像素）的 97%——**整段已唱文本几乎全部落在半透明的羽化渐变里，永远到不了实心色**，看上去就是“只染了一半”。现在按 OSD 自己的字号收敛到 `getFontHeight() * 0.42`（实测 6.61 像素，约一个汉字的 46%），配置滑块仍然有效，只是不再越过这条上限。
  - **羽化带的重复叠加**。羽化过去固定切 10 条，每条各自再画一圈 8 次偏移的光晕，也就是同一个字形在前沿附近被画最多 90 遍；`ScissorClipManager` 又会把裁剪框向外取整到整屏幕像素，窗口还在爬升时（字词刚开始唱）每条带子不足一个屏幕像素，取整后互相重叠，同一批像素上叠十几层半透明字形，结果就是前沿过曝糊成一团。现在：光晕整段只画一次（裁剪到真实已唱区域），羽化带只画字形；条数改成按它在屏幕上真正占几个像素来定（每条不少于 2 个屏幕像素，最多仍是 10 条），默认配置下是 6 条 × 2.20 像素。
  - **已唱的字被永久留在一个非整数缩放上**。旧实现用 `smoothStep(characterProgress)` 当强调量，唱完后它恒为 1，于是整段已唱文本长期停在 1.085 倍。字形图集是按 2 倍超采样、再以精确 0.5 倍缩小来保证锐利的，任何额外的非整数倍率都会重采样成一团发虚的墨；而且每个字是绕自身中心放大的，放大后必然压到相邻字上，于是既模糊又带重影。现在改成脉冲：`sin(π · smoothStep(进度))`，唱到一半时鼓起来、唱完立刻回到 1.0 倍恢复锐利（探针核对进度 0/0.5/1 对应 1.0/1.0625/1.0 倍）。
  - 缩放锚点也从取整过的 `getHeight()` 改成 `getFontHeight()`：锚点偏离字形真实中心会让放大后的字形与裁剪框在纵向上错开半个像素。
  - **可见行为变化（如需回退请说）**：唱过的字不再保持放大，只在唱到的瞬间鼓一下。这是为了换回锐利度——保持放大会让已唱段落长期处于重采样状态，而且已唱与未唱两半字号不一致。
- **侧栏选中项的强调色高亮框不显示**：高亮条的布局挂在它自己的 `beforeRenderCallback` 上，而 `AbstractWidget#renderWidget` 在跑这个回调之前就会因为 `isHidden()` 直接返回（父组件的渲染循环同样会跳过隐藏的子组件），于是"唯一能把它重新显示出来的代码"位于"隐藏后就不再执行的回调"里 —— 一次隐藏即永久隐藏。侧栏重建后的第一帧，列表与条目都还没跑过各自的回调、宽高全是 0，高亮条立刻判定为不可见并从此再不出现。现在布局改由侧栏自身的每帧回调驱动（一定会执行），因此隐藏可以被撤销；同时补上"几何尚未就绪就不落位"的判断，避免高亮条把一组全 0 坐标当作动画起点、之后从侧栏左上角滑进来。选中底色也从与悬停相同的中性灰改为主题强调色，"选中"和"鼠标经过"不再长得一样。
- 同一类问题还让私人 FM 的"换一首"按钮永久隐藏：播放条是常驻组件，首帧通常不在私人 FM 会话里，按钮在自己的回调里把自己隐藏掉之后就再也拿不到帧。现在显示与否由播放条的每帧回调判定。
- **逐字歌词的"放大强调"只放大了遮罩、字形本体没跟着放大**，表现为同一个字出现两份大小不同的拷贝（重影），并且已唱部分与未唱部分在交界处对不上（脱节）。OSD 桌面歌词与全屏歌词都存在，但成因略有不同，已分别修掉：
  - 全屏歌词：一个字词原来要画三层——未唱底色（`alpha*.5`，原始尺寸，直接画在 Minecraft framebuffer 上）、唱完后的文本（原始尺寸）、以及 KTV 合成层（离屏 FBO 里的字形 + 前沿遮罩，用 `Shaders.STENCIL` 合成）。`karaokeScale` 只套在第三层上，前两层保持原始尺寸，于是放大的高亮层下面永远压着一份没放大的同一个字。现在缩放倍率与锚点在字词开头就一次算好（`clampScaleAnchorX` 的视口约束照旧），该字词后续每一次直接绘制都套用同一份变换；FBO 纹理宽度也改为复用同一个向上取整后的宽度，保证纹理里的字形、遮罩前沿与最终合成四边形三者宽度完全一致。布局推进量仍是未放大的，所以换行不会抖动。
  - OSD 桌面歌词：原来叠三层——整行未唱底色（原始尺寸）、按填涂前沿裁剪的高亮层（原始尺寸，逐片段绘制）、逐字放大的强调层（放大尺寸，再画一遍同一个字形）。同色字形被画了两遍且尺寸不同 → 重影；这个字还没唱到的部分仍由原始尺寸的底层提供 → 脱节。现在改为逐字单遍渲染：底色、高亮和裁剪矩形都在同一个逐字缩放矩阵内产生，遮罩必然跟着字形一起放大，被删掉的强调层不再额外画一遍字形（每个字的绘制次数因此不增反减）。
  - 同时修掉了 OSD 的两处隐性错位：逐字步进原来把"单字宽度"直接相加，丢掉了 `CFontRenderer` 的 kerning（`drawString` 是带 kerning 推进的），沿行累积后高亮层会逐渐与字形错开；现在按"前缀宽度之差"步进。填涂前沿的羽化窗口改为按整个 timed word 片段计算并允许跨字边界，所以改成逐字绘制后前沿的软边依旧连续，不会在每个字里各自重新淡入一次。
  - 分段几何抽成 `KaraokeAnimationMath.solidRight` / `featherStrip` 两个纯函数并做了离线验证（4 种字宽组合 × 4 档羽化宽度 × 2120 个前沿采样）：实心段与羽化带两两不重叠（最大重叠 0.0 px）、覆盖长度等于真实前沿（最大误差 0.04 px，来自 0.05 px 的亚像素绘制阈值）、羽化不透明度沿前沿方向单调。

### 说明

- 已验证常驻灵动岛：状态判定与内容格式化探针 32 项通过——三开关组合的条目数量与顺序、`describe` 的数值永不越出宽度模板（帧率 −5..4000、延迟 0..4000 全量扫）、宽度签名在帧率 0..999 × 延迟 0..9999 的全量组合下恒定不变（含未知值 `--`）、健康度四档阈值、时钟格式；显示判定真值表覆盖"常驻永不抢下载/完成停留/通知的展示位"（8 种事件组合全扫）与"关闭总开关时任何理由都不显示"；入场抑制只在常驻且已稳定时生效；常驻宽度对内容单调递增、始终落在基础宽度与屏幕预算之内（240..2560 逻辑宽 × 0.60..1.35 缩放全扫）、基础宽度滑块仍然有效（160 → 720）。抽表重构后的风格尺寸表用反射逐项比对重构前的原始数字，7 套样式的最小宽度 / 高度 / 侧边留白全部一致。HUD 编辑器布局探针 4 项通过：灵动岛分组高度正好是 6 行开关 + 12 个滑块，且 480 逻辑高的小屏也能滚到最后一行。
- 已验证歌曲行图标对齐：图标探针 25 项通过 —— `PlayerIconAssets` 七个常量全部是绝对路径且 `getResourceStream()` 真能打开（修复前 `favorite.png` 与另外六张一律抛 `MissingResourceException`，探针会直接失败）、七张贴图的墨迹在 96×96 画布里居中（容差 1 像素）、歌曲行三枚图标在 20 像素按钮里的墨迹高度差 ≤1 逻辑像素（实测 10.00 / 10.50 / 10.50）、`favorite.png` 的墨迹比例与 `TARGET_INK_RATIO` 相差 ≤0.04。字形一侧用 `sun.misc.Unsafe` 造 `CFontRenderer` 外壳绕开纹理图集，因此贴图与字体两条路径都能离线量，不必进游戏。
- 尚未验证（需要进游戏确认）：两路 deck 同时出声的实际听感、新增控件的渲染与点击命中、结构混出点在真实曲目上的效果、对唱多行块在 HUD 与面板里的实际排版、常驻状态栏在各套灵动岛样式下的实际观感与左侧图标改动后的清晰度。

## [1.1.0] - 2026-08-22

以播放体验为主的中型更新：加入无缝切歌与手动"下一首播放"，把第三方聚合音源接入并补上兜底保护，同时修掉一批导致无法播放和灵动岛卡死的问题。

### 新增

- **无缝切歌（Automix）**。下一首在当前曲目播放中就完成解析、下载、解码，放在第二个 deck 上待命，切歌变成重叠淡化，而不是"关闭播放器 → 等待 → 下载 → 解码"的静音间隙。
  - 混出点优先取最后一句歌词结束 + 1.5 秒尾音；没有时轴歌词时做尾奏结构检测；都不可用时退化为 `时长 − 重叠`。混出位置被限制在曲末 12 秒或曲长 40% 以内，且歌词推算出的混出点一旦落在该窗口之外会被直接丢弃（而不是贴到边界上），不会把歌切短。
  - 混入点跳过空前奏（上限 12 秒且不超过曲长 12%），并吸附到检测到的第一个强拍；交接点对齐到淡出曲的小节线。
  - 重叠时长 1.5–10 秒可调（默认 4 秒），提前 10 秒开始预备，失败最多重试 3 次。第二个 deck 有独立内存上限（120 MB 与 PCM 上限一半中的较小值），超限曲目直接放弃预备并退回普通切歌。
  - 配置写在 `config/muonium/automix.json`，播放模式按钮右键开关。关闭时播放路径与旧版本完全一致。
- **DJ 级过渡处理**。
  - 低频交换：新增 `FilterLowShelf`（RBJ 低架双二阶 JSyn 滤波器，0 dB 时位精确旁路、增益缓变防跳变）与 `BassSwap` 立体声效果器，挂在各 deck 自己的信号链上。淡出侧低频在重叠前半段降到 −18 dB，淡入侧压在 −12 dB 直到它接管，低频始终只属于一首歌，避免两个底鼓叠在一起的浑浊感。
  - K 加权响度匹配：按 ITU-R BS.1770-4 计算重叠区响度（滤波系数按窗口自身采样率推导，400 ms 分块 + 绝对/相对门限），替代原来的 RMS 比值，避免重低音曲目被误判为更响、把音量补偿推向反方向。
  - 节奏锁定：两侧节拍网格都可测且接近时，把**淡出**那一路的速率弯到淡入曲的速度（限幅 ±6%，在重叠前 15% 完成），使两条节拍网格在整段重叠内保持对齐。只处理即将消失的一路，因为变速同时会移调。
  - 新增持久化开关 `bassSwap` / `tempoLock`，默认开启，暂无界面入口。
- **下一首播放**。每行歌曲新增一个控件（自绘图标），可自由指定接下来播放的曲目；多次点击按点击顺序排队；队列播完自动回到歌单原有顺序；当前没有播放队列时直接开始播放。单曲循环下会让位给排队曲目一次，automix 预备的曲目在队列变化时自动作废并重新预备。
- **GD 音乐台聚合音源**。音源选择新增二级菜单，按接口文档参数接入 netease / tencent / kuwo / tidal / qobuz / joox / bilibili / apple / ytmusic / spotify，严格遵守文档标注的 50 次 / 5 分钟额度。显示顺序按实测可用性排列，接口日后恢复无需改动代码。
- GD 曲目缺少时长元数据时，用解码得到的真实长度补齐列表、封面流与进度显示。
- GD 歌词兜底：主源没有 LRC 时，按标题与艺术家跨源匹配一次，命中与未命中都会缓存（bilibili 从不返回歌词）。
- **对唱歌词多行显示**。歌词时间轴上真正重叠的行（交集 ≥ 0.5 秒）会被归为一组，整组同时点亮、各自跑自己的逐字进度，并作为一个整块居中，而不是只点亮其中一行、让"当前行"在两位歌手之间来回跳。
  - HUD 与播放器面板都生效：HUD 把整组在视口内垂直居中（单行模式下也会完整显示这一组——该模式的意义是隐藏没在唱的行，而对唱时两行都在唱）；面板把整组抬起半个额外行高，使其以原有锚点为中心，并让失焦模糊按到"活跃块"的距离计算，避免同时点亮的另一行被糊掉。
  - 防误判：除 0.5 秒的绝对交集外，交集还必须占到较短那一行的 45% 以上。解析 TTML 时一行的窗口会顺延到它自己的和声唱完，而和声常常唱进下一行，只看绝对交集会把和声密集的歌整首误判成对唱。同时限制一组最多 3 行，链条更长时视为该段行窗口不可信，整段退回普通单行显示；间奏占位行永不参与分组。
  - 分组在歌词提交时（`LyricTimelineSupport.prepare`，插入间奏行并排序之后）算一次并写在行对象上，渲染层每帧只读不算。
- **AMLL TTML 逐字歌词库**（api.amll.dev）。人工校对的 Apple Music 风味 TTML，音节级时间轴带翻译与罗马音，在歌词解析链里排在最前面：命中它之后不会再请求任何平台歌词，普通 LRC 只作兜底。
  - 匹配顺序按接口文档：先用平台 ID 精确取（网易云 → `ncmMusicId`，QQ → `qqMusicId`（song mid），GD 的 netease/tencent/apple/spotify 子源各自对应 ID 参数），取不到再按曲名搜索并在本地打分。曲名归一化后必须相等或互相包含、且歌手能对上才会采用——错歌的逐字歌词比没有歌词糟糕得多。
  - 和声（`ttm:role="x-bg"`）追加到所属行末尾而不是新开一行：本项目的歌词模型没有独立的背景人声通道，新开行会与主行时间重叠，滚动和高亮都会跳。行窗口会顺延到最后一个和声词的结束（和声常常唱到下一行开始）。
  - 兜底保护：请求间隔不低于 25 毫秒（文档上限 50 次/秒）；404 视为"词库没有这首歌"并短期负缓存，不计入故障；连接失败/超时/429 连续三次打开熔断，5 分钟内不再请求；命中缓存保存原始 TTML 文本而不是渲染对象，避免跨次播放带入上一次的滚动状态；线程被中断时立即返回。
  - 负缓存只写"词库确实没有这首歌"（404 / 搜索无命中）。接口不可用时不写负缓存：否则一次网络抖动就会让这首歌在接下来的十分钟里被降级成平台歌词，而这条链路的优先级必须始终高于官方平台，官方只是兜底。连续失败由熔断兜住，不会因为不缓存而反复重试。
  - 只有真正逐字（至少一行拆出两个以上计时音节）才会被采用，行级 TTML 交还给普通歌词链路。解析禁用 DTD 与外部实体，任何解析失败都退回普通歌词。
  - 新增开关 `Prefer AMLL Word-by-Word Lyrics`（默认开启），关掉后完全回到旧的解析链。
- **QQ 音乐 QRC 逐字歌词**。AMLL 词库是人工投稿、覆盖率有限，因此在它之后再加一条逐字链路：QQ 自己的 QRC 是音节级时间轴，覆盖面比任何开放词库都大。最终顺序是 **AMLL TTML（逐字）→ QQ QRC（逐字）→ 普通歌词（兜底）**，两条逐字链路都拿不到东西时才会退回行级 LRC。
  - 能直接拿到 QQ 数字 songid 的曲目（QQ 源、GD 的 tencent 子源）直接用它请求；否则按"曲名 + 歌手"搜索并在本地打分，判定标准与 AMLL 那条链路一致（曲名归一化后相等或互相包含、且歌手能对上），同分时用时长差做取舍。错歌的逐字歌词比没有歌词糟糕得多。
  - 接口返回的 lyric / trans / roma 三段都是密文，需要先按 QQ 变体 DES 三重解密再 inflate。这个变体与 FIPS 46-3 有三处差异（每 4 字节半区字节序翻转、PC2 的 D 半区索引少减 1、两个 S 盒条目不同），任何标准 DESede 实现都只会输出垃圾，因此新增 `QrcCipher` 按公开的 FIPS 表加这三处互通偏差自行实现。
  - 解出的 XML 里 `LyricContent` 属性才是正文，且属性值内含原始换行，用 XML 解析器读会被规范化甚至报错，改为按字符串切并优先以 `"/>` 收尾，歌词里出现未转义引号时不会被截断。
  - 翻译与罗马音按行首时间就近贴到逐字行上（容差 1 秒），两种格式（普通 LRC 与带音节时间的 QRC）都能解析，`//` 这类占位行会被忽略。
  - 只有真正逐字（至少一行拆出两个以上计时音节）才会被采用：纯音乐、只有行级歌词的曲目会被拒绝，交回给带翻译的平台兜底链路。
  - 兜底保护与 AMLL 一致：最小请求间隔 200 毫秒、连续三次失败打开 5 分钟熔断、命中/未命中分别缓存 6 小时 / 30 分钟且只缓存原始文本（渲染对象带动画状态，不能跨次播放复用）、接口抖动时不写负缓存、线程被中断立即返回、任何异常都只是"没有逐字歌词"而不会影响播放。
  - 新增开关 `Prefer QQ Word-by-Word Lyrics`（默认开启）。
- **五套新配色主题**。缃云晴川（缃云黄 `#F4DC84` × 晴川蓝 `#79BEDF`）、沧溟玉釉（玉釉杏 `#FAF2E0` × 沧溟蓝 `#0E61AC`）、霜纨岫烟（霜纨白 `#FCF9E8` × 岫烟青 `#00B7C7`）、玫瑰月白（月白色 `#E8F0F2` × 玫瑰粉 `#D87888`）、青岚云脂（云脂色 `#FBF1D7` × 青岚色 `#73AE52`），全部为浅色系，主题总数增至 15 套。
  - 每套都按十个主题颜色配置项配全，而不是只换强调色。浅色底上强调色取同色系深一档、原色留给悬停态：离线探针按 WCAG 相对亮度校验，正文对比度均在 10:1 以上、次要文字 3.6:1 以上、强调色 3.8:1 以上，避免"能看见但读不清"。
  - HUD 编辑器新增「主题」分组，可直接点色块切换（每格显示该套的页面底色与强调色，当前项描白边），也可点标题行按顺序轮换。切换沿用播放器原有的 950 毫秒颜色过渡与涟漪动画。
- **液态玻璃灵动岛样式**。第 7 款样式，玻璃本体是半透明冷色基底叠强调色竖向渐变，上半部有折射亮带、底部有强调色回弹反射，一条两侧渐隐的镜面反光缓慢横扫；外沿一圈按时间旋转色相的动态取色光（四角取自主题强调色的四个色相相位），图标托盘是上亮下暗的磨砂反光。
  - 加载动画单独重绘：外圈 18 段静止玻璃刻度作参照，主环是 12 段沿色相环推移的彗尾（越靠尾部越短越淡），内圈三点反向慢转形成视差，中心光点按呼吸缩放。
  - 全部笔触都用圆角矩形与其渐变版本绘制，天然贴合灵动岛轮廓，不会像径向光斑那样溢出边界，也没有额外的 GL 状态切换。
- **灵动岛动画可配置**。HUD 编辑器「灵动岛」分组新增弹出速度、收起速度、内容切换速度、入场时长、入场回弹、加载转速六项。速度类是倍率（1.00× 即历史默认），折算后的插值系数被钳在 (0, 0.95] 内，因此倍率拉到最大也不会越过目标值来回抖动；入场时长 160–1200 毫秒，回弹可以调到 0 完全关闭。
- **主题调色板**。在侧栏主题按钮上右键打开二级菜单，十行分别对应播放器每次换主题时真正会变的那十个颜色配置项（页面底色、控件底色、控件悬停、主要文字、次要文字、强调色、强调悬停、输入框底色、导航底色、描边）。
  - 色相/饱和/明度三条滑块，轨道本身就是该通道的取值范围；拖动过程只改内存所以整个播放器实时跟着变色，松手才写盘，不会每帧写一次配置文件。
  - 调色按主题各存一份（`muoniumplayer_theme.json` 的 `custom` 段），换主题不会互相污染，切走再切回来还在。单项右键或「撤销此项」回到预设色，「恢复主题原色」清空当前主题的全部调色。
- **歌曲动态封面**。网易云的动态封面实际上是一段几秒的循环 MP4（接口字段 `videoPlayUrl`），项目此前只能解 GIF/APNG，遇到视频直接跳过，等于这个功能从来没生效。现在用 ffmpeg 把视频抽成有限帧，包进已有的 `AnimatedCoverTexture` 循环播放。
  - 生效范围是"当前播放的这一首"：桌面歌曲信息 HUD、全屏歌词页的大封面、播放器底部播放条的缩略图。歌单列表里的每行仍然是静态封面——几十行同时解视频既没必要也扛不住。
  - 抽帧管线是 `ffmpeg -i <video> -vf fps=10,scale=256:256,pad -f image2pipe -c:v png -`：PNG 在字节流里自定界（签名 + 一串"长度 + 类型 + 数据 + CRC"分块直到 IEND），顺着分块头走就能切帧，不需要容器解析也不额外引入依赖。补边色是全透明而不是黑色，非 1:1 的素材不会在圆角封面里露出两条黑边。
  - 与参考实现不同，这里**只解一次、解成有限帧**，而不是让 ffmpeg 常驻、每帧上传一次纹理：封面循环只有几秒，常驻进程会在整局游戏里持续占 CPU，而每帧上传必须发生在渲染线程上。
  - 资源上限：10 fps、256×256、最多 50 帧（5 秒，约 12 MB 帧数据）、视频下载上限 12 MB、整体解码超时 30 秒。同时只保留当前与上一首的动态封面纹理，更老的连 GL 纹理一起回收（`TextureManager` 自己从不淘汰，不主动回收的话每播一首歌就多留十几 MB）；被回收的那首会撤销"已尝试"标记，以后再播到它还能重新取。
  - ffmpeg 查找顺序：`-Dmuonium.ffmpeg` / `MUONIUM_FFMPEG` / `FFMPEG_PATH` → 游戏目录下的 `config/muonium/bin/ffmpeg(.exe)` → PATH → 各平台常见安装位置（含 winget 的 Links 目录与 `LOCALAPPDATA\Programs\ffmpeg\bin`）。找到的候选会用 `ffmpeg -version` 带超时校验一次并缓存；找不到时只提示一行、保持静态封面，并且每 5 分钟才重新找一次（用户可能在游戏运行期间才装上）。
  - 开关在两处：HUD 编辑器新增「封面」分组，里面是「动态封面」开关；模块面板里的 `Animated Cover` 是同一状态的另一处入口（默认开启）。真实来源是 `HudConfig.animatedCoverEnabled`，随其它 HUD 设置一起持久化，两处显示始终一致，旧配置文件里没有这个键时按开启处理。
  - 已经探测过但没找到 ffmpeg 时，HUD 编辑器里的开关标签会直接写成「动态封面 · 未检测到 ffmpeg」，免得用户开着开关却看不到动画还以为是坏了。这一行只读缓存结论，不会在渲染线程上真的去启动子进程探测。
  - 没装 ffmpeg 不是错误路径：只提示一行、撤销「已尝试」标记（装好后再播到这首歌还能取），静态封面照常显示；视频损坏、下载不全、解码超时同样只返回空帧列表，不抛异常、不影响播放。
  - 补上两处只有在 ffmpeg 卡住时才会暴露的隐患：抽帧的 30 秒超时原来只在两帧之间判断，进程卡住却不关 stdout 时读操作会永久阻塞并一直占着线程池的一条线程，现在由守护看门狗到点销毁进程，读端随即以 EOF 正常收尾；`ffmpeg -version` 校验原来先把输出读到 EOF 再等超时，遇到同名但不退出的程序同样会永久阻塞，现在排空放到守护线程上，超时判断一定能生效。
  - 一并修掉 `AnimatedCoverTexture` 上三个会让动画彻底跑不起来的缺陷：
    - 以 `clearable=true` 调用父类构造，而 `DynamicTexture` 首次上传后会把 `dynamicTextureData` 置空，换帧时往空数组拷像素会直接 NPE。
    - **构造期间的虚方法派发**：父类构造函数里的 GL 分配会走 `bindTexture() → getGlTextureId()`，而这个方法被覆写成「先换帧再返回纹理 ID」，派发回来时 `frames` 字段还没赋值，于是构造函数里就抛 NPE——动态封面纹理一次也没能建起来（离线探针复现：`NullPointerException at advanceFrameIfDue`）。现在换帧逻辑对未初始化状态直接返回。
    - **上传期间的重入**：`updateDynamicTexture()` 自己会绑定纹理，于是再次回到 `getGlTextureId()`，而「该换帧」的条件此时仍然成立，导致无限递归（离线探针复现：`StackOverflowError`，深度 1024）。现在先推进下一帧时间戳再上传，并用一个标志位挡住重入，一次调用只换一帧。
  - 动态封面的日志此前几乎是静默的，无法区分「这首歌没有动态封面」和「取动态封面失败」。现在两种情况各留一行，解码异常打印异常类名而不是只打 `getMessage()`（NPE 的 message 是 null，等于什么都没说）。
  - 装载时改为比较 `stableKey` 而不是 `Music` 引用：歌单重载与 automix 交接都可能让「同一首歌」换成另一个实例，按引用比会把刚解好的封面误判成「已经换歌」而丢掉。
- **下一首播放列表**。控制栏右下角新增开关（图标角标显示待播数量），展开后在播放器底部弹出抽屉，按播放顺序列出用户队列。
  - 按住整行上下拖动即可改变顺序：被拖起的一行略微放大并浮到其它行之上，其余行用弹簧动画让位，松手后自己缩回原样；拖到列表上下边缘会自动滚动，新入队与出队的行分别淡入淡出。
  - 排序只走 `CloudMusic.moveQueuedNext`，只搬动当前曲目之后、队列尾部之内的元素，因此正在播放的曲目、队列长度和播放模式都不受影响；每次改动都会让 automix 按旧顺序预备的那一路作废并重新预备。
  - 抽屉范围内的点击与滚轮由它自己吃掉，不会穿透到下层歌单；ESC 优先收起抽屉。
- 音质选择菜单改为动画展开/收起：面板从按钮上沿向上生长，选项随面板边沿扫过逐个显形，收起走同一条动画而不是瞬间消失，收起过程中不再接受点击。
- 侧栏栏目切换改为一枚共享的高亮胶囊在两项之间移动：两条边以不同速度跟随，移动中被拉长、到位后收拢，并按侧栏可视区域裁剪。
- **开启网易云听歌历史同步前的模态确认**。这是一次隐私授权，因此点击开启不再直接写配置：页面压暗、居中弹出确认框，标题"开启后会上报数据到网易云"，说明列出会被采集的电脑唯一标识符（CPU、系统 OS、网卡、硬盘序列号、IP 地址等，全部用于上报网易云客户端），下面是取消与确认。确认层位于所有播放器内容之上并独占鼠标与键盘，处理之前下层界面完全不可操作；点击遮罩外侧或按 ESC 等于取消。关闭开关是收回授权，仍然立即生效。
  - 确认框的说明文字改为按卡片宽度自动换行、卡片高度随行数增长（上限 8 行，超出在末行截断），原来固定一行的版式会把这种多行说明裁掉。字形是按需上传的，未上传时宽度为 0，因此字形就绪前每帧重算换行，避免整段被算成一行。标题过长时降一号字而不是截断。

- **暂停 / 继续快捷键**，注册在原版按键绑定里。位置是「选项 → 控制 → MuoniumPlayer」，和模组已有的打开音乐界面 / 编辑 HUD / 上一曲 / 下一曲 / 音量加减并列，默认小键盘 `*`（和音量的小键盘 `+` `-` 凑成一组媒体键，原版没有占用），键位由玩家在原版界面里自己改，冲突提示也由原版负责。
  - 用原版 `KeyBinding` 而不是模组自己轮询键盘，是因为这个模组本来就是独立 Forge mod、已经在注册这一组按键：键位跟着 `options.txt` 走（换整合包、重装模组都不丢），出现在玩家找快捷键时第一个会去看的地方，也不需要模组再写一份"捕获按键"的界面。
  - `KeyBinding.isPressed()` 消费的是 Minecraft 自己维护的按键计数，天然是上升沿触发（按住不重复），并且只在没有打开界面时被喂给这些计数，因此聊天框、指令栏、搜索框里打字不会误触发——这两点原先都要靠自己维护上一帧状态加 `currentScreen == null` 判断才能得到。也不再碰 `Keyboard.next()` 之外的任何 LWJGL 事件读取，不存在和 Minecraft 的按键分发抢事件导致偶发丢键的风险。
  - 播放器界面的空格键、控制栏按钮与这枚快捷键收敛到同一个 `CloudMusic.togglePlayPause()`，三处的判空与暂停语义不会再各自演化；正在进行的无缝淡化由原有的淡化驱动一并挂起。按下后灵动岛给出"已暂停 / 继续播放"提示，没有正在播放的歌曲时提示"当前没有正在播放的歌曲"，避免用户以为按键没生效——这些提示走普通队列，不会打断下载或转码卡片。
  - 随之移除的东西：HUD 编辑器的「快捷键」分组（分组标题恢复为五个）、`HudConfig.pauseHotkeyKeyCode` 及其读写、模组自有的每 tick 键盘轮询。旧配置文件里残留的那个键会在下一次写盘时自然消失。

- **私人 FM 预备下一首**。私人 FM 一次只从接口拉一首，旧流程是"当前这首播完 → 才去请求下一首 → 拿到之后重开一条播放线程"，中间横着一次网络请求加一整轮下载解码的静音；更要紧的是队列里从来只有正在播的这一首，无缝切歌预测下一首时恒为空，**automix 在私人 FM 下等于从未生效**。现在当前曲目播到 5 秒（或已逼近尾声、再不预备就赶不上）时就在后台把下一首推荐拉回来接到活队列尾部，automix 随即像普通歌单那样提前解析、下载、解码并交接，手动按下一首也走同一条无缝捷径。
  - 5 秒不是 0：用户连点跳过时每首都立刻打一次接口纯属浪费，还会把推荐白白塞进接口自己的去重表。也不必赶在 10 秒的预备窗口之前——队列长出下一首会递增 `queueRevision`，automix 看到版本变化就会立刻重新预测并开始预解码，所以晚到的预备一样有效。
  - 纯预备动作，边界刻意收得很紧：不提交 trash（那只属于用户点"跳过"）、不起播、不改面板上正在显示的推荐、失败也不覆盖用户能看到的状态文案。后台预备用独立的在途标志，不与用户触发的刷新 / 换模式 / 跳过共用同一把锁，两者不会互相饿死。
  - 兜底保护：每首歌最多预备 3 次、失败后冷却 15 秒；未登录、正在等用户处理播放失败、用户正在手动切歌时都不预备。预备结果回到主线程才动队列（界面线程正在遍历同一个 `ArrayList`），并且要同时满足"仍在 FM 会话中、队列还是当初那条实例、队尾就是正在播的这一首、这一首的 ID 与队尾不同"才会接上；任何一条不成立就当没预备过，旧的"播完再请求下一批"路径原样生效。
  - 有了预备之后 FM 的推进不再每首都经过一次批次请求，因此在两条起播路径（普通起播与无缝交接）上补了面板同步，否则私人 FM 页面会一直停在上一首。灵动岛在接上时给一条普通队列提示（"已预备下一首"），排在用户触发的反馈之后，不会打断下载或转码卡片。
  - 顺带把私人 FM 的"上一首"改成无条件不响应。原先的判断是 `curIdx <= 0`，在实践中等于永远拦住（FM 每首歌都是一条独立的单曲队列）；预备会让队列真的长出历史，不改的话 FM 会凭空多出一个官方客户端里并不存在的回退按钮。

### 改进

- **「下一首播放」控件换用图标字体**（圆角播放三角 + 加号角标，fontello U+E309）。字形以字体形式随包发布（`muonium/fonts/player-queue-icons.ttf`），走的是项目里其它图标字体（icomoon / music / music-brand-icons / qq-music-icons）的同一条渲染链，仍可跟随主题染色。歌曲行与播放条右下角的队列开关共用同一个入口，观感一致。
  - 此前的做法是把同一个字形离线烘成 96×96 贴图：那张位图把字形撑到画布的 76%×87.5%（相邻贴图图标一律是 54%×50%）并把三角右下裁掉一角，实际显示是一个又大又缺角的图形，也就是"图标没生效"的原因。改成字体后没有这一步离线栅格化，字形按实际字号在运行时生成，播放器缩放到任何大小都不会失真；那张坏掉的 `play-next.png` 与它的 `PlayerIconAssets.PLAY_NEXT` 入口一并删除。
  - 码位单独成一个字体文件而不是并入既有的 `fontello.ttf`：后者已经占用了 U+E001..U+E004、U+E800..U+E802、U+F127、U+F234，混进同一个文件迟早撞码。这也是 `music-brand-icons` 与 `qq-music-icons` 各自独立成包的原因。
  - **按墨迹居中，而不是按字距框居中**（修掉这枚图标偏上约 2 像素、与相邻两个按钮对不齐的问题）。`IconWidget` 原来的落点用 `getStringWidth()` 与 `getFontHeight()` 计算，它们是排版度量：正文靠它们对齐基线，但单个图标要的是墨迹落在按钮正中。两者在这里差了三层——`getFontHeight()` 是 `(ascent + descent − 8.5) / 2`（8.5 是为正文排版调出来的经验值），字形位图却是按完整的 `ascent + descent` 烘的，`drawString` 还额外把整张位图上移 2 像素，而图标的墨迹在位图里本来也不居中。新增 `GlyphInk`：照着 `GlyphGenerator` 一模一样的参数（同字体、同 `ceil(advance + max(0, −lsb))` 宽、同 `ascent + descent` 高、同基线落点）离线再烘一次，扫 alpha 求墨迹包围盒，`IconWidget` 拿到它之后直接把墨迹中心摆到按钮中心，实测残差 0.00 像素，并且不再依赖任何经验常数——字号、字体、乃至那个 8.5 以后怎么变，结果都自己跟着变。
  - 这也顺带修掉了原先那份 −2.13 像素的纵向修正：它只补了 8.5 那一项，漏掉了 `drawString` 里的 `y −= 2`，净效果是把图标整体抬高 2.005 像素。
  - **两处按钮各用自己的字号**（修掉播放条上那一枚偏大的问题）。字形是按字号栅格化的，原先歌曲行（20 像素）和播放条右下角（18 像素）共用同一个 38 号渲染器，18 像素那处墨迹占到 58.3%，比 20 像素那处的 52.5% 明显更胖。现在按按钮边长挑字号：目标是墨迹高度占边长 52%，取自相邻贴图图标的实测值（`favorite.png` 96×96 画布、墨迹 52×48；`playlist.png` 墨迹 52×50，即 54.2% 宽、50.0%–52.1% 高，且在画布里严格居中）。20 像素挑 38 号（52.5%）、18 像素挑 34 号（52.8%），两处互差 0.3 个百分点。
  - 挑选是运行时按实测墨迹做的，不是写死的尺寸表：以后换字形、加一档字号或改按钮大小都不需要重新校准常数。度量拿不到时（AWT 不可用、字体缺失）退回默认的字距框居中并用最大的一档字号——位置差一点，但按钮照样画得出、点得动。`GlyphInk` 的结果按 (字体, 字符) 缓存，失败也缓存，不会每帧重试；这份可选度量默认不启用，项目里其它图标字体的位置都是在字距框居中的前提下调好的，不受影响。
- 下一首播放列表新增删除：每行把手左侧多一个移除按钮（自己吃掉点击，按下去不会变成拖动），标题栏右侧新增「清空」；移除走新增的 `CloudMusic.removeQueuedNext(int)` / `clearQueuedNextSongs()`，只删当初插进来的那几条，队列尾指针同步下移、清空后复位，正在播放的曲目与后续顺序都不受影响，删完立刻淡出而不是等下一次快照对齐。
- 下一首播放抽屉收窄到播放器宽度的 62% 并居中，整体高度（含表头）封顶在播放器高度的 60%，不再占掉大半个播放器界面。
- **手动"下一首"也走无缝交接**。此前只有歌曲自然播完才会交叉淡化，用户自己按下一首仍然是"关闭播放器 → 重新解析下载解码"的静音断点。现在只要下一首已经预解码待命，手动切歌就复用同一条交接路径。
  - 请求由界面线程发出，但只置一个标志，真正的交接仍然发生在播放线程的 10 毫秒监督 tick 上，与自动切歌走同一段 `performAutomixHandover`，因此不会出现两个线程同时替换 player / session 的竞态。
  - 淡化长度单独取 900 毫秒，而不是按小节算出来的 1.5–10 秒：那个长度对一次用户没要求的过渡是正确的，但用来回应一次按键会显得迟钝。淡出曲剩余不足时仍然收敛到原有的 700 毫秒补救斜坡。
  - 兜底层层设防：无缝切换关掉、下一首还没备好、备好的那一首不是队列此刻预测的下一格、正在暂停、上一次淡化还没跑完，任何一条不满足都原样退回旧的切歌路径；请求发出后备用轨在这几毫秒里失效的话，会在播放线程上补做一次普通切歌，绝不会出现"按了下一首但什么也没发生"。判定必须排在 `canPlayNext()` 之前，否则它在循环模式走到队尾时先把当前索引改成 -1，而交接要到下一个 tick 才完成，中途读取该索引的界面会越界。
  - 顺带修掉一处既有的重复上报：交接时对上一首的听歌时长上报与播放次数统计原来发生在新 deck 真正出声之前，一旦之后的会话校验或起播失败而提前返回，这一首会在自然播完时被再算一次。现在这段挪到索引切换的位置，只在交接确实成立后执行。
- **搜索结果起播改为接"最近播放"**。从搜索结果点一首歌开始播，后续不再顺着搜索关键词的排序往下播——那是搜索引擎的排序，不是用户在听的东西。现在队列是"所选歌曲 + 刷新过的网易云最近播放"，与主流播放器在这个位置接用户自己的播放上下文一致。
  - 刻意先起播、再异步补队列：拉一次最近播放要走一次网络，让点击等它回来会有几百毫秒的"点了没反应"。补队列时会校验活队列还是当初装进去的那一条实例、并且仍然只有那一首，用户在这段时间里点了别的歌、换了歌单或者用了"下一首播放"就直接放弃追加；追加后递增队列版本号，让 automix 丢掉按"没有下一首"做出的预测并重新预备。
  - 最近播放为空（未登录、Cookie 失效、接口结构变更）时退回用搜索结果本身续上，也就是旧行为；去重按"来源 + 来源内 ID"，所选歌曲不会因为出现在最近播放里而排两次。
  - 只有搜索框产生的列表走这条路径。热搜、排行榜、专辑、最近播放这些发现页复用了同一个"曲目已在内存里"的标记，因此新增了一个独立标记加以区分，这些页面的起播行为完全不变。
  - 最近播放的解析从发现页的私有方法搬到播放层的 `NeteaseRecentPlaysService`，界面与播放链路读同一份实现。解析仍然不写死响应路径（网易云在 `data` / `list` / `allData` 与 `{song:{...}}` 之间变过多次），而是深度优先找出看起来像歌曲的对象，找不到就当"没有最近播放"。
- 网络超时分层：音频流单独使用 15 秒连接 / 45 秒读取，不再套用 8 秒的通用默认值，慢速 CDN 不会再被判定为"无法播放"。
- GD 请求预算：调用方已放弃的解析（超时取消会中断线程）立即停止，不再为没人读取的结果消耗额度；被取消的尝试也不再被记为音源故障。
- 搜索结果为空时不再写入缓存，避免 15 秒内的重试同样落空；bilibili 偶发返回空数组时，在预算充足的前提下对用户所选音源多给一次机会。
- 灵动岛新增超时结束机制：加入歌单 20 秒、转码进度 30 秒无变化、没有专属终止消息的常驻卡片 30 秒。同一状态频道重复上报不再顺延截止时间。
- automix、下一首播放、网易云听歌时长上报的提示统一按普通优先级排队，不再打断用户主动触发的通知。
- AAC 解码在超出 PCM 上限时中止并给出原因，不再先写满缓存、再在分配大块堆内存时失败。

### 修复

- bilibili DASH 纯音频（.m4s 分片 MP4）无法播放：补齐分片 MP4 的 AAC 解码，并按 host 发送流请求头，解决 bilivideo CDN 一律返回 403 的问题。
- joox 音源卡在转码阶段无法播放。
- 音源选择切回"官方源"后仍然使用第三方源搜索。
- 主页刷新功能失效；刷新与同步歌单失败后灵动岛长时间不消失。
- 配置项"灵动岛基础宽度"调节无效。
- 模态弹窗内滚动会穿透到下层歌单与歌词页，出现两层一起滚：滚轮此前只拦下了其中三个弹窗，音源中心被漏掉，歌词面板拿到的还是原始滚轮值。
- 播放解析失败时不再自动跳到下一首，改为停下等待用户操作。
- `AudioSample.amp()` 没有同步父类字段，而 `SoundObject.play()` 会重新套用该字段（始终是默认的 1.0），导致每次开始播放都是满音量——交叉淡化要求淡入 deck 从 0 起，这个问题会让淡化完全失效。
- 立体声窗口读取错误：`AudioSample.read(int, float[], int, int)` 把帧当作交错值处理，实际只返回一半窗口、起点也减半、后半段填零，导致 automix 的节拍与响度分析全部基于错误数据。
- `AudioSample.pause()` 会把效果器从电路中摘除却不通知效果器本身（`isProcessing()` 仍返回 true），暂停后恢复会让低频交换在混音中途静默失效；现在按实际挂载状态重新连接。
- 淡化过程中的暂停/恢复、晚触发（预备完成晚于计划的交接点）、以及被切歌抢回为当前播放的 deck，都会正确复位低频增益与播放速率，不会把混音中的处理带进正常播放。
- **预备分析破坏正在播放的音频**（本次更新中最严重的问题）：播放器的采样并非全部载入内存，而是通过一个共享的 `RandomAccessFile` 加一个滑动窗口边播边解码。automix 的节拍/响度分析直接从**正在出声的**采样上跳读任意位置，把这个窗口挪到了音频线程脚下，引擎线程随即抛出 `ArrayIndexOutOfBoundsException: -12666884`（日志中一次预备触发 175 次）并停止产生音频，表现为歌放到一半就没声、后续曲目也放不出来。现在分析改由新增的 `PcmWindowReader` 完成：它自己打开一份只读文件句柄解析 WAV（RIFF/fmt/data 分块、`WAVE_FORMAT_EXTENSIBLE`、16/24/32 位 PCM 与 32 位浮点），与播放路径完全隔离，任何异常都返回空数据而不是抛出。
- **用上一首的歌词算出本首的混出点**：歌词加载失败时只是不提交结果，全局歌词列表仍保留前一首的时间轴，而混出点推算没有归属校验，于是每首歌都按别人的歌词提前混出（日志中两首歌均恰好被切掉 30 秒）。现在歌词列表带曲目标识，只有确认属于当前淡出曲、且推算终点不超过其时长时才会采用。
- **无缝切歌在交接瞬间仍会顿一下**，原因有三处，都在交接点上：
  - `AudioSample.cue()` 内部会走 `stop()`，而 `stop()` 会把效果器从电路里摘掉、`SoundObject.play()` 又不会重新接回去（Processing 封装自己留了 TODO）。`playFrom()` 原来是先挂低架滤波再 cue，等于每次混音的淡入侧都在**完全不滤波**的状态下出声：两首歌的低频全额叠加，交接处发闷发撞。现在改成先 cue、再挂滤波器。
  - 淡入 deck 原来是在交接那一刻才启动。解码后的 WAV 并不驻留内存，而是由 `RandomAccessFile` 通过 4096 采样（约 46 毫秒）的滑动窗口在**引擎线程**上边读边播，于是一次文件寻道 + 8 KB 读取 + 单元挂载 + 电路改线全部落在正在渲染另一路的音频回调里。现在预备阶段（提前几十秒、在预备线程上）就完成 cue、挂滤波器和首个窗口预热；再在计划交接点前 400 毫秒让这一路以**全静音**开始出声，交接瞬间只剩增益变化。
  - `AudioPlayer.dispose()` 在 `stop()` 之前调了 `jump(0)`，而 `AudioSample.jump()` 会清空队列后**启动播放**——等于在淡化收尾时又往运行中的引擎里塞了一个声音单元，位置正好是交接点。已去掉。
- 预备好的淡入 deck 会跟随暂停状态一起停/续，避免它在用户暂停期间独自跑过对齐好的小节线；若用户此时往前拖动进度条，这一路会重新回到静音待命位置，不会带着错误的播放位置进入下一次交接。
- 单曲循环且没有排队曲目时不再与自己交叉淡化，会完整播放到自然结束后再重头开始。
- **第三方源"下载完成却不播放"**：播放线程只捕获 `Exception`，而解码链路会抛 `Error`。随 mod 打包的 jflac 是按需加载的，FLAC 只有带标签时才第一次用到 `VorbisComment` / `VorbisString` —— 网易云的无损文件不带标签，第三方源（GD）的普遍带标签，所以运行期间重新编译或替换过 mod 文件后，这些还没加载过的解码器类再也读不到字节码（`NoClassDefFoundError`），看起来就像"只有第三方源不能播放"。该 `Error` 会一路逃出 `PlayThread.run()` 把播放线程杀死：没有任何失败提示、`playing` 停在 true、灵动岛也不收尾，于是下载完成后就一直没有声音。
  - 播放初始化（取流/下载、创建播放器、声道不匹配后的重试）统一改为捕获 `Throwable`，失败照常走"无法播放"提示并停下等待用户操作。
  - `PlayThread.run()` 增加最后一道防线：任何漏网的 `Throwable` 都会提示一次、复位 `playing` 与"等待用户操作"状态，播放线程不再静默死亡。
  - 解码器抛 `LinkageError` 时给出可操作的提示（"音频解码器加载失败，请重启游戏后重试"）而不是抛一个类名。注意这类失败与歌曲本身无关：不要在游戏运行期间执行 `gradlew build`。
- 全屏歌词的当前字词放大后被歌词视口切掉一块（行首最明显）：逐字高亮以字词中心为锚点放大，而行首字词的左边缘正好压在视口裁剪边界上，一半放大量落在视口外。现在缩放锚点会被约束在视口内——能按中心放大就按中心，贴边的字词把锚点收拢到边界、只向内侧生长，布局坐标不变所以不会引起换行抖动；同时字词纹理宽度改为向上取整，不再削掉最后一个字形的右边缘。

### 移除

- LX Music（lxmusic）自定义音源支持。
- `MusicLyricsPanel` 中失效的 gitee TTML 抓取代码（结果被丢弃、提交回调已注释，实际从未生效），由上面的 AMLL 接入取代。

### 说明

- 版本号历史上出现过 1.0.2 → 1.0.4 → 1.0.0 → 1.0.1 的回退，`MuoniumPlayerMod.MOD_VERSION` 还停留在 1.0.0。本次将 `build.gradle`、`MuoniumPlayerExtension.VERSION`、`MOD_VERSION` 三处统一到 1.1.0。
- 已离线验证：DSP 探针 27 项、下一首播放队列 14 项、JSyn 挂载探针、PCM 窗口读取与混出点探针 20 项全部通过（含 16/24/32 位与浮点 WAV 的逐样本比对、越界截断、歌词混出点的采纳与丢弃判定），`gradlew compileJava` 构建成功。
- 已验证 AMLL 接入：TTML 解析探针 35 项（真实词库样本逐样本比对英文分词、日文罗马音、和声追加、时钟格式、XXE 拒绝、行级 TTML 判定）与联网探针 15 项（网易云 ID、QQ mid、GD 子源 ID、搜索兜底、错歌手拒绝、404 不触发熔断、缓存复用）全部通过。
- 已验证 QQ QRC 接入：联网探针覆盖 5 首曲目（中文、日文带翻译 + 罗马音、英文、纯音乐、不存在的曲名），逐字音节数、时间轴单调性与"音节不越出行窗口"逐行校验通过；另有 7 项行为探针（缓存命中 10 毫秒且返回新的行对象、QQ 数字 songid 直连、GD tencent 直连、其它 GD 子源走搜索、线程中断立即返回且保留中断标记、错歌手拒绝、纯音乐无逐字时干净退回）全部通过。渲染部分与其它歌词一致，无需额外验证。
- 已验证无缝交接修复：接缝探针 17 项 + 交接时序探针 7 项在真实音频引擎上通过——预备后的 deck 保持静音、滤波器在 cue 之后仍在链上、流窗口预热到指定采样点、按 10 毫秒轮询模拟的交接中淡入 deck 恰好在计划点到达计划入点（2000 毫秒对 2000 毫秒），且没有额外分配第二个声音单元；首个窗口读取由冷读 96 微秒降到 1 微秒（这一读原本发生在音频线程上）。
- 已验证动态封面：ffmpeg 定位探针（游戏目录私有 bin 优先于用户安装位置，两种都命中）与抽帧探针通过——4 秒 1:1 素材 40 帧、16:9 素材补成 256×256 且四角 alpha=0 / 中心 alpha=255、20 秒素材被截到 50 帧 / 12 MB、损坏与不存在的文件返回空列表且不抛异常、ffmpeg 诊断信息被完整读走（管道不会堵死子进程）；URL 选择与写盘上限探针通过（只有 videoPlayUrl 时图片分支为空、同时给图与视频时各取所需、超限写盘返回 0）。纹理上传与实际观感需要 OpenGL，只能进游戏确认。
- 已验证对唱分组：归组探针 26 项通过（真实对唱、普通顺序歌词、和声尾巴误判、0.5 秒边界、间奏行、零长度行、三行组、超长链拒绝、多组独立编号、越界与空表、重新归组不留残值，以及经 `LyricTimelineSupport.prepare` 的端到端标记）。渲染改动本身需要 OpenGL，无法离线验证。
- 尚未验证（需要进游戏确认）：两路 deck 同时出声的实际听感、新增控件的渲染与点击命中、结构混出点在真实曲目上的效果、对唱多行块在 HUD 与面板里的实际排版。
