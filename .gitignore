# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle

# other
eclipse
run

# local claude settings (user-specific, not for sharing)
.claude/settings.local.json

# original Muonium reference source — not part of this mod's distribution
Muonium/

# Codex/local agent state
.codex/
.codex-optifine-adload.html

# JVM crash dumps and runtime diagnostics
*.hprof
hs_err_pid*
replay_pid*
*.log
logs/
crash-reports/

# Local archival snapshots are kept outside the repository.
backups/

# Local player state, account credentials and downloaded media
NCMCookie.txt
MusicCache/
muonium_player_config.json
muonium_player_theme.json
muonium_hud.json

# Other editor-local state
.vscode/
# Legacy config names kept ignored so migrated user files are never committed.
tritium_player_config.json
tritium_player_theme.json
deuteriummusic_hud.json